project('valacraft', ['vala', 'c'],
  version: '0.1.0',
  default_options: [
    'warning_level=3',
    'default_library=static'
  ]
)

# Add src directory
subdir('src/core')
subdir('src/client')
subdir('src/server')